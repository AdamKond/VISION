# Media trzymane jako zwykłe pliki git (bez LFS — niezawodne na obu maszynach).
# 'binary' = bez konwersji końców linii i bez diffów tekstowych.
*.mp4  binary
*.mov  binary
*.webm binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.heic binary
*.gif  binary
*.mp3  binary
*.wav  binary
